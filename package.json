{
  "name": "sunstone-pricing-calculator",
  "version": "1.0.0",
  "description": "B2B pricing/quoting tool for Sunstone Logistic Systems \u2014 SaaS subscription, hardware, implementation, rental, and a client-safe quote view. Calculations transcribed and verified against Sunstone_Pricing_Calculator.xlsx. Runs locally (Node + SQLite) or on Cloudflare (Pages + Functions + D1).",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node --no-warnings server.js",
    "test": "node test/verify.js && node test/qa-engine.js",
    "cf:schema:local": "wrangler d1 execute sunstone-quotes --local --file=schema.sql",
    "cf:schema:remote": "wrangler d1 execute sunstone-quotes --remote --file=schema.sql",
    "cf:dev": "wrangler pages dev",
    "cf:deploy": "wrangler pages deploy public",
    "test:workbook": "node test/verify.js",
    "test:engine": "node test/qa-engine.js",
    "test:api": "node test/qa-api.js",
    "test:ui": "node test/qa-ui.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
